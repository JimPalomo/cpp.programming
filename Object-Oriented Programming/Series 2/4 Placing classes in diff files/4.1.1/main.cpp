#include <iostream> 
#include "Burrito.h"
using namespace std;

int main() 
{
    // Don't forget linking error that occurs if files are not linked
    // Ex. g++ main.cpp Burrito.cpp -o main (must be called this way) 
    Burrito bo;

    return 0;
}